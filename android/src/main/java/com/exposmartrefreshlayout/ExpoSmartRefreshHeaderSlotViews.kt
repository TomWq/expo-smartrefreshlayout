package com.exposmartrefreshlayout

import android.content.Context
import com.facebook.react.views.view.ReactViewGroup

/** Marker host used to distinguish custom refresh-header content from scroll content. */
internal class ExpoSmartRefreshHeaderSlotView(context: Context) : ReactViewGroup(context)
